aws STS
aws signing keys using v4
service endpoints
named profiles
~/.aws/credentions (used to store access key and secret key)
~/.aws/config (generic info like preferred region)
cli env variables
aws cli autoprompt